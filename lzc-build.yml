manifest: ./lzc-manifest.yml
pkgout: ./
icon: ./icon.png

compose_override:
  services:
    manifest:
      read_only: true
      security_opt:
        - no-new-privileges:true
      cap_drop:
        - ALL
      mem_limit: 1g
      pids_limit: 256